name: Lint

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pylint pytest pandas streamlit matplotlib seaborn google-cloud-bigquery python-dotenv imbalanced-learn scikit-learn

      - name: Get changed Python files
        id: changed_files
        run: |
          files=$(git diff --name-only origin/main...HEAD | grep '\.py$' || true)
          echo "files=$files" >> $GITHUB_OUTPUT

      - name: Run pylint
        if: steps.changed_files.outputs.files != ''
        run: pylint ${{ steps.changed_files.outputs.files }}

      - name: Run tests
        run: PYTHONPATH=. pytest tests/unit/test_usecase3.py tests/unit/test_usecase3_app.py
